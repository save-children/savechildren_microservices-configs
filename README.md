# savechildren_microservices-configs

properties used by savechildren orphanage application mircoservices
savechildren orphanage application config server will resolve configuration using git (github).
